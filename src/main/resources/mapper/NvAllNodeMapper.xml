<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.team.mapper.NvAllNodeMapper">
	<insert id="create">
		insert into novel_allnode (nvall_id, nv_id,
		nvall_descendantnode,mem_id)
		values
		(#{nvall_id},#{nv_id},#{nvall_descendantnode},#{mem_id})
	</insert>

	<select id="findByNvID" resultType="io.team.domain.NovelAllNode">
		SELECT * FROM novel_allnode
		where
		nv_id =
		#{nv_id};
	</select>

	<select id="findByUerID"
		resultType="io.team.domain.NovelAllNode">
		SELECT * FROM novel_allnode
		where
		mem_id =
		#{mem_id};
	</select>

	<select id="findByDescendantNode" resultType="_int">
		SELECT nv_id FROM novel_allnode
		where
		nvall_descendantnode =
		#{nvall_descendantnode};
	</select>

</mapper>
